{
  "affected_artifacts": [
    "apps/web",
    "packages/web-contracts",
    "packages/github"
  ],
  "added": [
    "packages/github (@sos-2/github): the provider-neutral GitHub/project adapter — the typed GitHubPort contract (connection with least-privilege OAuth/GitHub-App repository scope — the CONNECTION CONTRACT, not a live vendor flow; repository discovery; branch/revision selection; empty-repository detection; snapshot/metadata import; webhook registration where supported; branch/commit/pull-request operations), an injectable HTTP/request transport seam (GitHubRequestPort — no network call is ever made by the package), a typed capability surface where unsupported operations answer explicit typed UNSUPPORTED outcomes (never silent failures, never fabricated successes), an in-memory deterministic reference implementation whose states are explicitly simulated, a transport-backed read-path provider over a scripted/injectable request port, and configuration through @sos-2/infra-deployment's environment contract (the GitHub-scope variables GITHUB_ACCESS_TOKEN/GITHUB_WEBHOOK_SECRET read from an INJECTED source record, never ambient process.env; names pinned by test against the actual frozen registry; secret values never echoed). ZERO declared dependencies of any kind (the P3 lockfile byte-identity precedent; the test toolchain is borrowed at run time from the frozen W17 adversarial suite). Honest status: the real-system connection is NOT_YET_CONNECTED (validation GitHub account pending); connection evidence is NEVER fabricated — configured credentials are PREPARED, never CONNECTED",
    "packages/web-contracts/onboarding: the onboarding view-model contracts — pure types + pure projections following the P1 core/ discipline (zero domain logic, zero DOM; every vocabulary imported from the owning @sos-2/* packages through @sos-2/web-contracts' dependency surface — mission content, uncertainty classes, grant permissions, truth states, spine identities; never redefined). The greenfield journey model (purpose → outcomes → stakeholders → measures → constraints → review → authority confirmation → GitHub connection, each step a typed stage with inputs, typed validation results, uncertainty and the next allowed action; the review stage defers to @sos-2/mission's own validateMissionContent — the single validation authority), the brownfield journey model (repository/runtime import → evidence scan with the frozen truth states and first-class state blocks → competing architecture hypotheses presented as a set with their uncertainty, never collapsed → confirmation), the advanced raw-JSON import mode (typed, clearly secondary, same REVISE-class authority gate), the persistence engine (a completed journey formalizes through the frozen domain builders into a Mission artifact, a spine-minted ImplementationModel record, a SystemState whose implementation entry carries the exact repository revision { kind: 'git-sha', value }, observational Evidence records and typed trace links), and the revision-pinned DEMO world (static literal timestamps, no Date.now/Math.random/fetch, spine-minted deterministic identities, the base-head fixture revision)",
    "apps/web/onboarding: the onboarding product surfaces — the hub (greenfield/brownfield/advanced with the honest connection panel), the greenfield wizard (step-by-step, RESUMABLE BY URL through native GET forms — zero client JavaScript; typed validation findings as role=alert blocks; the authority confirmation as an explicit checkbox gate naming the REVISE permission; the GitHub connection step showing the provider-neutral connection state honestly — SIMULATED reference provider badged, the real system NOT_YET_CONNECTED; the persisted step showing the exact spine ids and the linked repository revision under the DEMO badge), the brownfield flow (import → scan with distinct truth states and the PARTIAL coverage state block → the competing hypothesis set with uncertainty → the confirmation gate), and the advanced raw-JSON surface (clearly secondary). Fully server-rendered from the onboarding view models; a11y per the P1 shell standards (labels/legends, 44px touch targets, landmarks, sr-only context); the P4 test suites run through the packages/github package's orchestrating test script (the P3 borrowed-toolchain precedent) because apps/web's and web-contracts' frozen test scripts only include their own P1 directories",
    "apps/web/onboarding/ARCHITECTURE-DELTA.json: this Architecture Delta record"
  ],
  "removed": [],
  "modified": [],
  "preserved_invariants": [
    "the frozen W0-W18 core and every merged Wave-1 file are untouched: spec/, docs/, scripts/, .github/, pnpm-lock.yaml, pnpm-workspace.yaml, packages/web-contracts/{package.json,tsconfig.json,core/**}, apps/web/{package.json,next.config.ts,tsconfig.json,postcss.config.mjs,shell/**,app/layout.tsx and every existing route}, packages/live-store, packages/api-contracts, apps/api and infra/deployment are byte-identical to the base head; all P4 changes are additive under the three owned paths plus the declared thin route wrappers",
    "the Semantic Spine remains the ONLY identity/envelope/trace authority: onboarding artifacts are built exclusively through the frozen domain builders (createMission, createSystemState, createEvidence, createTraceLink) with spine-minted deterministic identities; the repository identity in System State is a spine sos://ImplementationModel/<hex> id and the revision is the frozen { kind: 'git-sha', value } shape consumed from @sos-2/system-state — never redefined, never re-minted (pinned by tests); brownfield hypothesis records reference spine ArchitectureGraph ids; no second semantic registry, no invented artifact kinds",
    "demo fixture state can never render as live state: every fixture-backed onboarding view model carries data_source { kind: 'DEMO', label: 'DEMO — SIMULATED DATA', fixture_revision } (the label is the P1 contract text, validated and pinned); the simulated GitHub connection carries simulated: true through to the view (CONNECTED_SIMULATED can never render as CONNECTED_REAL — pinned by the alignment test); the fixtures are static, revision-pinned to the P4 base head and deterministic (no Date.now, no Math.random, no fetch, no ambient process.env — pinned by source-scan tests)",
    "the 6 evidence truth states remain distinct everywhere: the brownfield scan phases render SUCCESS/PARTIAL/UNKNOWN distinctly and the coverage state block is the first-class P1 model (a page never shows a blank hole); UNSUPPORTED stays an explicit typed provider outcome, never a silent failure and never conflated with UNAVAILABLE/UNKNOWN (pinned by the adapter contract tests)",
    "authority honesty: formalizing a mission, importing repository state into System State and the raw-JSON path all require the explicit REVISE-class confirmation gate (from the frozen GRANT_PERMISSIONS vocabulary) — the gate never closes itself, never evaluates grants (that is @sos-2/authority's) and never widens authority; bodies/providers cannot mint authority",
    "honest NOT_YET_CONNECTED: the real-system GitHub connection is NOT_YET_CONNECTED throughout this Work Order (validation account pending); configured credentials are PREPARED, never CONNECTED; the transport provider REFUSES handshakes instead of fabricating them; the simulated reference provider is explicitly marked on every state it produces",
    "zero new external dependencies and the lockfile is unchanged: packages/github declares ZERO dependencies of any kind (the P3 root-cause rule, pinned by its own checker), the onboarding subpath adds no dependency edges (it consumes @sos-2/web-contracts' existing dependency surface), and pnpm-lock.yaml + pnpm-workspace.yaml are byte-identical to the base head",
    "determinism: no network at build or test time (the reference provider is in-memory; the transport provider only ever uses the injected request port; no next/font, no images, no runtime fetch in render paths), the full verification suite (verify-repo, verify-productization, pnpm -r --if-present build, pnpm -r --if-present test) was run twice with identical results, and same-input renders are byte-identical (pinned by the render determinism tests)"
  ],
  "boundary_changes": [
    "thin App Router route wrappers under apps/web/app/onboarding/** (the P1 convention, each ≤ ~10 lines, importing only from the P4 owned paths and awaiting the async searchParams): page.tsx (the hub), greenfield/page.tsx, brownfield/page.tsx and advanced/page.tsx (the three journeys, searchParams-forwarding)",
    "new zero-dependency workspace package at packages/github (covered by the existing packages/* glob in pnpm-workspace.yaml — no workspace file change): package.json (ZERO dependency fields, P3 precedent; scripts borrow the vitest/tsc toolchain at run time from the frozen W17 adversarial suite), tsconfig.json (Node >= 22.18 native TypeScript type-stripping lineage, noEmit), vitest.config.ts (root pinned via import.meta.url, globals, seed 424242) and scripts/check-zero-deps.mjs",
    "the packages/github package test script ORCHESTRATES the three P4 test suites (github adapter contracts, the onboarding view-model contracts under packages/web-contracts/onboarding, and the onboarding render smoke tests under apps/web/onboarding) through their own P4-owned vitest configs after building @sos-2/web-contracts' dist — because apps/web's frozen test script only includes shell/test/** and web-contracts' frozen test script only includes core/test/**, this is the only no-frozen-file-change wiring that lets `pnpm -r --if-present test` run the P4 suites",
    "the onboarding subpath is consumed as SOURCE: apps/web/onboarding and the P4 test suites import packages/web-contracts/onboarding/src via relative paths (extensionless, bundler-resolved) because the frozen @sos-2/web-contracts package exports only the P1 core dist (adding a subpath export would require touching the frozen P1 package skeleton); the subpath is typechecked by apps/web's next build and by its own borrowed-tsc config — it is NOT part of the package's dist build",
    "test-side source imports of frozen packages (read-only, never modifications): the journey integration test imports @sos-2/live-store, @sos-2/mission, @sos-2/system-state and @sos-2/evidence sources to exercise the real repositories and domain validators end-to-end, and the environment pin test imports @sos-2/infra-deployment and @sos-2/system-state sources to machine-pin the GitHub-scope variable names and the 'git-sha' revision kind against the frozen registries (a rename there fails the P4 suite loudly)",
    "no changes to pnpm-lock.yaml, pnpm-workspace.yaml, .github/, spec/, scripts/ or any frozen source: the P4 additions introduce no new dependency edges, no new CI workflows and no semantic changes to any merged Wave-1 surface"
  ],
  "rationale_ref": "spec/productization-work-orders/P4-onboarding.md",
  "work_order": "P4"
}

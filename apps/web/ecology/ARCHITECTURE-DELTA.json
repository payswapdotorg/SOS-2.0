{
  "affected_artifacts": [
    "apps/web/ecology",
    "packages/web-contracts/ecology",
    "apps/web/app/packages/page.tsx",
    "apps/web/app/history/page.tsx",
    "apps/web/app/experiments/page.tsx",
    "apps/web/app/packages/composition/[segment]/page.tsx",
    "apps/web/app/history/revision/[kind]/[segment]/page.tsx",
    "packages/web-contracts/package.json",
    "apps/web/package.json"
  ],
  "added": [
    "packages/web-contracts/ecology (the ./ecology subpath of @sos-2/web-contracts): the P10 deep view models — PURE types + PURE projections with ZERO domain logic and ZERO DOM dependencies. (1) ecology-vm: the packages workspace — PackageEcologyVM/CompositionVM/PackagesWorkspaceVM where contextual applicability (EVERY context-conditioned estimate verbatim with its own uncertainty class, context and sample), retained failure refs, assurance obligations and diversity families are structurally VISIBLE (validators reject a projection that drops them; there is deliberately no single applicability score), and compositions carry their OWN independent evidence (the own-evidence verdict is computed by the OWNING authority @sos-2/composition evaluateOwnEvidence — member evidence surfaces as FOREIGN, never folds in; independence assessments are the owner's JustifiedCombinedProbability records). (2) history-vm: the revision TIMELINE (supersede chains per lineage: kind + root, oldest -> newest, dangling entries still shown) + the revision DIFF (presentational field-level comparison with per-field restored_from detection — including the exact parameter restore of a rolled-back meta change — and full-content exact_restore_of). (3) evolution-vm: the self-evolution READ-ONLY projection — the MetaProcess revision chain (parameters verbatim + digest per revision), the non-disableable governance guard state (GOVERNANCE_INVARIANT_IDS and EVOLVABLE_KEYS verbatim from @sos-2/meta-evolution, retained typed rejections), the retained failure memory (@sos-2/memory entries grouped by kind, FAILURE/ROLLBACK/LIABILITY/LEARNED_RULE never deleted), learned rules and the machine-state snapshot. (4) live-read: the ONE impure module — the live-state read adapter reading every family through @sos-2/live-store repository ports (missions/contexts/systemStates/evidence/architecture/candidates/assurance/experiments/decisions/authorityGrants/packages/history.memories/history.hypotheses/developmentState) plus the demo seeding of the in-memory reference store THROUGH the repositories' own validated put calls. (5) demo/demo-ecology-world: the deterministic, revision-pinned DEMO fixture extending the P1 world (package v2 revisions declaring composition participation, a composition chain FORMING -> VALIDATED with own composed-system evidence about the composition's own chain id, a meta-strategy package, the process chain baseline -> retired trial -> exact restore, a guard rejection produced by the owning package's pure evaluator, the retained failure memory, a correlational causal hypothesis and a machine-state snapshot) — all constructed EXCLUSIVELY through the frozen domain packages' own builders, static literal timestamps, spine-minted deterministic ids.",
    "apps/web/ecology: the P10 product surfaces. (1) view-state/ecology-data: the deterministic bridge — builds the demo world, seeds the reference live store through the repositories, reads canonical live state back through readEcologyLiveState and projects every workspace view model (memoized; no Date.now/Math.random/fetch/fs anywhere in the render path). (2) shell/pages/packages-workspace: the Packages workspace — the repertoire with diversity families, the compositions with members/roles/typed wiring/independence justifications and the OWN-evidence verdict, per-package discovery cards with contextual applicability, per-estimate uncertainty, retained failures, assurance obligations, pre/postconditions, realizations and composition participation, the honest UNAVAILABLE state block for the not-yet-live PackageComposition store family, and the gated composition-building action (never a fake button). (3) shell/pages/history-workspace: the timeline of every revision chain from the live read (per entry: what changed, why/provenance, linked evidence, deep diff link) with the fixture-only kinds explicitly labelled. (4) shell/pages/revision-detail: the deep sub-route — the chain, the consecutive-revision field-level diffs with restored values highlighted, and the six-question rationale of the chain head (UNKNOWN for unknown chains, EMPTY for single-revision chains). (5) shell/pages/composition-detail: the deep composition view (members, wiring, own-evidence verdict, applicability, obligations, rationale; UNKNOWN for unknown ids). (6) shell/pages/evolution-workspace (the /experiments route): the controlled evolution plane in BOTH directions — the canary experiment story (preserved from the same P1 view models via the frozen core projector over the live-read experiment record: stage, metrics/guardrails, stopping/rollback criteria, honestly-simulated evaluation, gated next step) AND the self-evolution surface (process chain with the exact restore visible, the non-disableable guard with the retained typed rejection, the retained failure memory, the learned rules, the machine state, the live-read provenance and the read-only note). (7) components/ecology-rationale: the six-question rationale section for P10 subjects. All surfaces reuse the P1 shell components (PageShell/Card/StateBlockView/chips/DemoBadge) read-only, render the DEMO badge on every fixture-backed card, expose what/why/evidence/uncertainty/authority/next on every consequential card, respect 44px touch targets, semantic landmarks and progressive disclosure (details/summary), and are fully server-rendered.",
    "apps/web/app/packages/composition/[segment]/page.tsx + apps/web/app/history/revision/[kind]/[segment]/page.tsx: the thin (<= 10 lines) App Router wrappers for the two new deep sub-routes, importing only from the owned paths under apps/web/ecology/**.",
    "apps/web/ecology/test/render-smoke.test.tsx: 24 deterministic render tests (async server components rendered through the streaming renderer) — workspace content, DEMO-label enforcement, state blocks (UNAVAILABLE/UNKNOWN/EMPTY), the guard rejection, the failure-memory entry kinds, the exact-restore marker, live-read provenance and byte-identical re-renders; apps/web/ecology/vitest.config.ts: the ecology vitest config (seed 424242) mirroring the shell config convention; packages/web-contracts/ecology/test: 54 deterministic tests — ecology projections (verbatim applicability/uncertainty/failure/assurance visibility, composition own-evidence incl. the member-evidence-is-foreign case, justified independence, diversity preservation), history projections (lineage chains, contiguity, sibling separation, the revision diff with restored_from and exact_restore_of, consecutive-revision enforcement), evolution projections (process chain order + digests incl. the exact restore, guard invariants verbatim, failure-memory retention, learned rules, machine state, live-read provenance), live-read discipline (verbatim round-trips through the repositories, honest empty-family counts, caller-supplied provenance) and determinism (byte-identical worlds, stores, reads)."
  ],
  "removed": [],
  "modified": [
    "apps/web/app/packages/page.tsx, apps/web/app/history/page.tsx, apps/web/app/experiments/page.tsx: the CONTENT of the three thin P1 route wrappers replaced (the explicitly sanctioned boundary change of Work Order P10) so the /packages, /history and /experiments routes render the P10 deepened workspace views; the wrappers stay thin (<= 10 lines) and import ONLY from the owned paths under apps/web/ecology/**. The frozen P1 page components under apps/web/shell/components/pages/** remain byte-identical and stay covered by the frozen P1 shell tests.",
    "packages/web-contracts/package.json (the package skeleton, NOT core/): the './ecology' subpath added to the exports map (types ./dist/ecology/index.d.ts, default ./dist/ecology/index.js); the build/typecheck/test scripts chained to also compile and run the ecology subpath (core first, then ecology — the emitted dist/ecology modules self-reference '@sos-2/web-contracts' for the P1 core vocabulary, which Node and TypeScript resolve through the package's own exports map); the workspace dependencies added for the owning vocabularies the P10 projections import and never redefine: @sos-2/architecture, @sos-2/assurance, @sos-2/composition, @sos-2/context, @sos-2/causal, @sos-2/live-store, @sos-2/memory, @sos-2/meta-evolution — all workspace:* (ZERO new external dependencies; the resolved external dependency set is unchanged). core/** is untouched.",
    "apps/web/package.json (the app skeleton): ONLY the test script chained to also run the ecology vitest config ('vitest run --config shell/vitest.config.ts && vitest run --config ecology/vitest.config.ts') so the P10 render tests run under the repository gate `pnpm -r --if-present test`. No dependency, build or dev-script change."
  ],
  "preserved_invariants": [
    "the frozen W0-W18 core and the merged Wave-1 files are untouched except the three declared thin-wrapper CONTENT replacements and the two package-skeleton wiring files listed in boundary_changes: spec/, docs/, scripts/, .github/, pnpm-workspace.yaml, every existing packages/* source (including packages/web-contracts/core/** and packages/live-store/**) and every existing apps/* source (including apps/web/shell/** and apps/api/**) remain byte-identical to the base head c9baa42a72d76246a19144bf1679b8687d5a2d62 (verified: git diff --stat pnpm-lock.yaml pnpm-workspace.yaml is empty; the only modified files are the five declared in modified/)",
    "the Semantic Spine remains the ONLY identity/envelope/kind/trace/serialization authority: the P10 view models never mint ids, never rewrite envelopes and never redefine a single vocabulary — every vocabulary (artifact statuses, truth states, maturities, applicability estimates, diversity profiles, assurance obligations, composition binding kinds, independence bases, memory entry kinds, liability severities/owner-kinds/resolutions, guard invariant ids, rejection codes, process parameters, state-block kinds, data-source provenance) is IMPORTED from the owning @sos-2/* package and carried verbatim (pinned by tests, e.g. the guard invariants and the failure-memory entry kinds)",
    "no score hides uncertainty, evidence quality or context: the package projections preserve EVERY context-conditioned applicability estimate verbatim with its own uncertainty class, context and sample size (an empty applicability list is REJECTED by the validator); retained failure refs and assurance obligations stay visible; compositions carry their OWN evidence with the independence locked invariant machine-enforced by the owning @sos-2/composition evaluator (member evidence surfaces as foreign, never folds in) — pinned by tests",
    "self-evolution reads canonical live state: the evolution/history/packages surfaces read their records through @sos-2/live-store repository ports (readEcologyLiveState over missions/contexts/systemStates/evidence/architecture/candidates/assurance/experiments/decisions/authorityGrants/packages/history.memories/history.hypotheses/developmentState) — never from fixture objects directly; even the DEMO dataset is seeded INTO the reference store through the repositories' own validated put calls, and the read round-trips are pinned verbatim by tests; where a family is NOT a live-store family at this base (PackageComposition, MetaProcess, MetaChange), the surface says so with an explicit UNAVAILABLE state block instead of passing the fixture off as live state",
    "the console observes self-evolution but never steers it: the EvolutionVM is a READ-ONLY projection (no mutation path exists in any P10 module; the read-only note is structural and pinned by tests), mirroring the W11 meta-view discipline",
    "demo fixture state can never render as live state: every P10 view model carries the P1 DataSource provenance with the exact DEMO_MARKER_TEXT and the pinned fixture revision (ECOLOGY_DEMO_FIXTURE_REVISION = the P10 base head), the DEMO badge renders on every fixture-backed card, and the demo-marker tests reject dropped/altered markers (the demo dataset structurally cannot produce LIVE-marked provenance)",
    "determinism: no Date.now / Math.random / fetch / process.env reads anywhere in the P10 render paths — all timestamps are static literals, ids are spine-minted deterministic content addresses, the fixture/store/read/projection pipeline is byte-identical on re-runs (pinned by determinism tests), the vitest seed is 424242, and `next build` statically generates every P10 route with zero network at build time",
    "pnpm-lock.yaml and pnpm-workspace.yaml remain byte-identical to the base head (git diff is empty); zero new external dependencies — the only dependency additions are workspace:* links to already-frozen @sos-2/* packages inside the modified packages/web-contracts/package.json; NOTE (pre-existing, NOT introduced by P10): the lockfile at the base head already lacks the importer entries for the P2/P3 workspace members (apps/api, packages/api-contracts, packages/live-store, infra/deployment), so `pnpm install --frozen-lockfile` fails at the base head c9baa42 and still fails at this head for that same pre-existing reason — the P10 dependency additions are workspace links only and change no resolved external package",
    "accessibility and shell standards are preserved: the P10 pages compose the frozen P1 PageShell (desktop rail, mobile header + bottom nav, sticky footer, skip link), use semantic landmarks, aria-current navigation state, 44px minimum touch targets, label+glyph status chips (never color-only), first-class state blocks (loading/empty/unknown/unavailable/partial/error where each applies) and rationale deep-links answering what/why/evidence/uncertainty/authority/next on every consequential surface"
  ],
  "boundary_changes": [
    "REPLACED (sanctioned by the Work Order): the CONTENT of the three thin P1 route wrappers — apps/web/app/packages/page.tsx, apps/web/app/history/page.tsx and apps/web/app/experiments/page.tsx — so the /packages, /history and /experiments routes render the P10 deepened workspace views from apps/web/ecology/shell/pages/**; the wrappers remain thin (<= 10 lines) and import only from the owned paths (the import specifier contains 'shell/' because the P10 page components live under apps/web/ecology/shell/pages/ — the ecology workspace's shell-composed pages — which also keeps the frozen P1 structural test that asserts wrappers delegate to a shell implementation directory)",
    "ADDED sub-route wrappers: apps/web/app/packages/composition/[segment]/page.tsx (the composition detail) and apps/web/app/history/revision/[kind]/[segment]/page.tsx (the revision diff detail) — thin (<= 10 lines), importing only from the owned paths; /experiments deepens in place (no new route) so the nav contract and the More index stay frozen",
    "MODIFIED packages/web-contracts/package.json (the P1 package skeleton — 'add your subpath' per the Work Order; core/ untouched): the './ecology' exports entry + chained build/typecheck/test scripts + the workspace:* dependencies for the owning vocabularies (@sos-2/architecture, @sos-2/assurance, @sos-2/composition, @sos-2/context, @sos-2/causal, @sos-2/live-store, @sos-2/memory, @sos-2/meta-evolution)",
    "MODIFIED apps/web/package.json (the P1 app skeleton): the test script chained to also run the P10 ecology vitest config, so the P10 render tests execute under `pnpm -r --if-present test`; no other script, dependency or configuration of the app changed",
    "the /experiments route now renders the deepened Evolution workspace (controlled experiment + self-evolution) instead of the P1 ExperimentsPage component; the frozen P1 component (apps/web/shell/components/pages/experiments-page.tsx) is preserved byte-identical, remains covered by the frozen P1 shell tests, and the canary experiment story itself is preserved on the new page (re-rendered from the same frozen core projector over the live-read experiment record) so no P1 product surface is lost"
  ],
  "rationale_ref": "spec/productization-work-orders/P10-package-history-evolution.md",
  "work_order": "P10"
}

{
  "affected_artifacts": [
    "tests/dogfood (@sos-2/dogfood): the integrated dogfood suite + the R1-R31 coverage manifest",
    "tests/adversarial (@sos-2/adversarial): the 13 frozen adversarial classes",
    "docs/evidence/dogfood/dogfood-report.md: the integrated scenario narrative with exact revisions, decision points (incl. ASK instances) and adversarial outcomes",
    "docs/evidence/dogfood/coverage-ledger.json: the machine-checkable R1-R31 requirement coverage ledger",
    "docs/evidence/dogfood/ARCHITECTURE-DELTA.json: this Architecture Delta record",
    "the W17 integrated semantic subgraph: one connected trace-linked component from the greenfield Mission through the realized SystemState and the brownfield loop's learned ecology updates to the self-evolution meta loop's final process revision and retained failure memory, joined by the typed W17 handoff links (brownfield ImplementationModel/declared architecture DERIVED_FROM the greenfield realization; ValueModel DERIVED_FROM Mission; Context CONSTRAINS the realized state; Mission v2 DERIVED_FROM Mission v1)"
  ],
  "added": [
    "tests/dogfood: ONE connected end-to-end dogfood scenario through the public exports of the three harness apps' underlying packages (@sos-2/greenfield W14 stages 1-6 with both decision variants — the ASK path with human resolution as the primary chain and the covering-grant ACT path as the decision point; @sos-2/brownfield W15 stages 1-9 over a snapshot DERIVED from the greenfield realization at the same exact revision, in both the nominal (EXPERIMENT outcome) and degraded (guardrail breach -> ROLLBACK with bounded recovery) worlds; @sos-2/meta-evolution W16 stages 1-7 over the SAME derived object fixture with all four meta-change outcomes — guard REJECT, ROLLBACK with exact restore, first-class ASK, ACT promotion), plus explicit mission evolution, a subordinated ValueModel with a surfaced CONFLICTS_WITH variant, a Context artifact, causal grounding (correlation -> CORRELATIONAL hypothesis through the only sanctioned path), the integrated trace-chain connectivity + directed-query assertions, rationale/evidence view models and whole-scenario determinism",
    "tests/dogfood/src/coverage.ts + tests/dogfood/test/coverage-ledger.test.ts: the machine-checked requirement coverage ledger twin — the dogfood suite renders its test titles from DOGFOOD_CASES, and the ledger JSON (docs/evidence/dogfood/coverage-ledger.json) must cover R1-R31 with zero gaps, reference only real test titles, equal the code-side manifest exactly, carry valid stage labels and exact in-scenario revision tokens, and leave no orphan cases",
    "tests/adversarial: the 13 frozen adversarial classes, each a dedicated suite that CONSTRUCTS the fault, asserts the system DETECTS or CONTAINS it with a typed rejection/failure record (never a silent pass, never a crash) and asserts the trace chain stays queryable afterward: stale System State, missing telemetry, contradictory evidence, unavailable dependency, malicious generated change, assurance monitor failure, rollback failure, package interaction failure, misleading confidence, architectural drift, diversity collapse, invalid authority, governance-weakening self-evolution (the W16 guard held under accumulation)",
    "pnpm-workspace.yaml: one added glob line 'tests/*' — the minimal test wiring that makes tests/dogfood and tests/adversarial ordinary workspace packages so 'pnpm -r --if-present build/test' includes their suites (the W17 task packet: 'extend the root vitest workspace so pnpm -r test includes your suites'); no package source, app, spec or script is touched by this entry"
  ],
  "removed": [],
  "modified": [],
  "preserved_invariants": [
    "no frozen contract is modified: spec/, docs/ (outside docs/evidence/dogfood), scripts/, apps/ and every packages/* source are untouched; all changes are additive under the owned paths tests/dogfood, tests/adversarial, docs/evidence/dogfood plus the single documented pnpm-workspace.yaml test-wiring line",
    "no core identifiers, envelopes, kinds, trace-link types, truth states, vocabularies or semantics are invented or redefined: every identity, artifact, link and vocabulary is CONSUMED through the merged packages' public exports (@sos-2/semantic-spine remains the only minting authority; the W17 connective tissue uses the spine's createTraceLink/deriveDeterministicArtifactId exclusively)",
    "the spine's identity discipline is preserved and demonstrated: trace links exist only between spine artifact ids (the learned TransferStore record is deliberately NOT trace-linked — store-queried instead), evidence truth states remain distinct (UNAVAILABLE is never folded), and every artifact in the integrated subgraph carries its exact revision",
    "zero new runtime dependencies: the two test packages depend only on workspace:* packages plus the repository's existing dev toolchain (typescript, vitest, @types/node); no production package's dependency graph changes",
    "pre-existing behavior is unchanged: all 44 pre-existing workspace projects build and pass their suites unmodified (2288 pre-existing tests green in both determinism runs)",
    "the evidence binds to the exact W17 head SHA (AGENTS.md rule 10) and the coverage ledger is machine-checked against the code that produces it — evidence without provenance or stale-head evidence is not accepted"
  ],
  "boundary_changes": [
    "two new TEST-ONLY workspace packages (@sos-2/dogfood, @sos-2/adversarial) joined through the added tests/* glob in pnpm-workspace.yaml: they consume the merged packages' public exports one-directionally and are consumed by nothing; no runtime boundary of any merged package changes",
    "the W17 handoff trace links are new typed edges between EXISTING spine artifact kinds (ImplementationModel/SystemState/ArchitectureGraph/ValueModel/Context/Mission pairs over the frozen DERIVED_FROM/CONSTRAINS types) — evidence-binding edges of the integrated proof, not new semantics; no new kinds, no envelope changes, no new link types"
  ],
  "rationale_ref": "spec/work-orders/W17-integrated-dogfood.md",
  "work_order": "W17"
}

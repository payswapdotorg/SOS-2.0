{
  "affected_artifacts": [
    "apps/web/app/page.tsx",
    "apps/web/app/start-surface.tsx",
    "tests/live-ux",
    "docs/evidence/production-connectivity/live-ux/discoverability",
    "pnpm-lock.yaml"
  ],
  "added": [
    "apps/web/app/start-surface.tsx: the root first-user discovery surface as a COLOCATED non-route module beside the thin route wrapper (the Next.js colocation pattern — it creates no route). RootStartSurface renders 'What are you trying to accomplish?' (the visible h1), the three accomplishment paths (Start something new -> /onboarding/greenfield, Bring an existing system -> /onboarding/brownfield, Resume or watch current work -> /mission), onboarding as a first-class entry ('New here? Start with onboarding' -> /onboarding — the P18 structural fix), and the five first-run understandings via native details/summary progressive disclosure (SOS watches continuously; SOS can summon bodies; the user's computer is optional; SOS asks when authority or evidence is insufficient; completion requires independent verification — each linking the real route where the user can SEE it). OverviewBody keeps the P1 overview composition (dominant hero, below-hero cards, active autonomous work) below the discovery surface for returning users, rendered from the same exported shell components + fixture view models. Fully server-rendered (no client components/fetches/scripts); 44px touch targets and the shell a11y discipline throughout; the discovery copy makes NO live/connected data claims of its own (provenance stays with the linked surfaces).",
    "tests/live-ux (@sos-2/tests-live-ux): the UX-discoverability + journey instrumentation suites. (1) ROOT-PAGE + ONBOARDING DISCOVERABILITY SPECS (run on-branch): the accomplish question, onboarding + mission entry discovery, the five understandings present through progressive disclosure, links-to-REAL-routes machine-checked against apps/web/app, server-rendered-only + thin-wrapper pins, a11y landmarks preserved, deterministic output; the first-user path followed click-by-click through the onboarding hub into the greenfield wizard's and brownfield import's first steps. (2) TWELVE COMPONENT-LEVEL JOURNEY SPECS (run on-branch): the P17-C LiveMissionPage rendered with scripted HONEST DTO states built through the DTO producer helpers (emptyLiveObservation / liveObservationFromDrain over scripted drain reports + connectivity snapshots with mixed CONNECTED/UNAVAILABLE/DEGRADED/UNKNOWN states) — fresh user, greenfield, brownfield, ASK, body summon, laptop-off/cloud execution, body interruption/replacement, promotion, rollback, observation-without-body, history, package learning — each verifying the user can FOLLOW the journey through the UI (visible entry points, next affordances, evidence/rationale links, honest states), never merely that a component exists; the consequential journeys additionally drive the SAME envelopes the forms POST through the real merged @sos-2/action-gateway (held/revoked/never-held authority gates, idempotent replay, smuggled-authority typed rejection, rollback evidence + verification) and ASK resolution through the merged @sos-2/ask queue (terminal Decision record). (3) ROUTE-LEVEL FULL-PATH SPECS (capability-gated): mission route -> live observed state -> consequential action -> receipt -> evidence against the P18-B mounts; while the mounts are absent every suite skips with the EXPLICIT reason requires-p18b-mount (describe.skipIf over an honest filesystem+source capability probe; an always-run probe reports the gate state; import.meta.glob registers only existing files so absent mounts never break collection); at integration ALL must be enabled and green before P18 completes — pass states are never fabricated. Structural pins: fixed vitest seed 424242, node environment, offline determinism, no ambient network/time/env in the suites.",
    "docs/evidence/production-connectivity/live-ux/discoverability: the machine-readable evidence records — README (narrative + reproduction), the per-journey discoverability record (what a fresh user CAN discover with the rendered-string/link-target/honest-state evidence, and the honest gap list including the un-routed live mission surface at base, the ASK section not linking /ask, the missing pause/resume/replace affordances, and the frozen-nav limitation), the journey-spec index (on-branch vs capability-gated inventory + the integration contract for enabling the gated suites), the secrets audit, and this architecture delta."
  ],
  "removed": [],
  "modified": [
    "apps/web/app/page.tsx: the root route rewritten from a bare <OverviewPage /> re-export into the thin wrapper composing the P1 PageShell with the P18-C discovery module (RootStartSurface + OverviewBody from ./start-surface) — still a thin wrapper of 10 non-empty lines importing from the shell, exactly as the frozen shell contract (apps/web/shell/test/navigation.test.ts) pins every shell route; the discovery surface sits INSIDE the shell's main landmark, ABOVE the kept overview body",
    "pnpm-lock.yaml: the mechanical importer addition for tests/live-ux ONLY (workspace links + the already-pinned react/react-dom/@types/vitest/typescript versions; +40 lines, no version changes); pnpm-workspace.yaml is UNTOUCHED (the existing tests/* glob covers the new importer)"
  ],
  "preserved_invariants": [
    "the frozen W0-W18 core and every merged P0-P17 file remain untouched: apps/web/shell/**, apps/web/onboarding/**, apps/web/ecology/**, apps/web/live-mission/** (P17-C, read-only), packages/** (including the frozen web-contracts navigation model — no nav-item changes: discoverability comes from the ROOT surface and in-page entries exactly as the work order prescribes), apps/web/app/mission/** and every other app route, infra/**, spec/**, scripts/, .github/** are byte-identical to the base head 3717b6c",
    "the FULL repository suite stays green exactly as at base (verified: pnpm -r --if-present build && pnpm -r --if-present test -> EXIT 0, zero failed files/tests; the repo contract checks verify:repo + verify:productization PASS)",
    "the frozen thin-wrapper contract holds: apps/web/app/page.tsx is 10 non-empty lines importing PageShell from the shell; the discovery surface is a colocated non-route file (start-surface.tsx — no page.tsx of its own, creates no route; pinned independently by suite 00 so the integration head cannot regress it)",
    "no fabricated discoverability: the root links only REAL routes that exist on this branch (machine-checked against apps/web/app by suite 00); the live mission route is NOT linked as live because it does not exist until the P18-B mount — recorded as an honest gap with the integration step, never papered over",
    "no fabricated pass states: the route-level suites skip with the EXPLICIT reason requires-p18b-mount while the P18-B mounts are absent; the always-run capability probe reports the gate state; at integration all 125 tests must be enabled and green before P18 completes",
    "server-rendered only, no client components: the root surface has no 'use client', no client fetches and no scripts in its output (pinned structurally; the Next build prerenders / as static content, 16/16 pages generated)",
    "progressive disclosure never loses provenance or uncertainty: the five understandings' full text stays in the server-rendered markup; every linked surface renders its own honest states, LIVE/DEMO provenance badges and the six product review questions (What is happening? / Why does SOS believe this? / What evidence supports it? / What uncertainty remains? / What authority is required? / What can happen next?)",
    "the discovery surface makes no live/connected data claims of its own (no LIVE badge, no CONNECTED claims — it is static guidance + links); the kept overview body retains its own honest DEMO badges",
    "the UI never mutates state directly: every consequential journey drives the SAME typed envelopes the live-mission forms POST, through the real merged action gateway (authority re-evaluated at action time, fail-closed; idempotent replay; smuggled authority fields typed-rejected) and ASK resolution through the merged AskQueue (human authority; terminal Decision record)",
    "determinism: fixed vitest seed 424242, fixed literals for every instant/id in the scripted DTO states, no ambient network/time/env in the suites; same inputs -> byte-identical renders (pinned)",
    "secrets discipline: no credential value appears in any owned file (the secrets audit records the scan); the suites are reference-mode UI journeys with zero real-provider calls"
  ],
  "boundary_changes": [
    "one new owned location: tests/live-ux (@sos-2/tests-live-ux — a tests/* workspace member covered by the existing pnpm-workspace.yaml glob, so pnpm-workspace.yaml is untouched; deps: @sos-2/action-gateway, @sos-2/ask, @sos-2/authority, @sos-2/decision, @sos-2/web-contracts — all merged, all read-only; devDeps: react/react-dom/@types/* + the existing typescript/vitest toolchain already pinned in the lockfile; the lockfile change is importer-addition only)",
    "the app surfaces under test are consumed through TEST-TIME ALIASES to their real source files (the tests/real-observation precedent): @web-app/root-page, @live-mission/*, @web-onboarding/*, @web-shell/* — no package dependency edges are created from or into apps/web; apps/web's package.json, test scripts and owned modules are untouched",
    "the root surface is delivered as page.tsx (thin wrapper, unchanged contract shape) + start-surface.tsx (colocated non-route module): the minimal deviation from the literal owned-path list ('apps/web/app/page.tsx'), forced BY the frozen thin-wrapper contract in apps/web/shell/test/navigation.test.ts (a shell-owned test this lane must not modify); the colocation creates no route and touches no other owned path — recorded for the architect in the evidence README",
    "the P18-B capability probe reads the filesystem (apps/web/app/live-mission/page.tsx, apps/web/app/api/live-mission/actions/route.ts, and the /mission page source for the LiveMissionPage pointing) — read-only inspection of sibling-lane mount points, no modification; the route-level suites load the mounts ONLY when they exist (import.meta.glob)",
    "no frozen-contract change was required: the perceived gap (no onboarding/live-mission rail items in the frozen navigation model) is NOT blocking — the work order prescribes root-surface discoverability, which this lane delivered; adding rail items remains an architect-level decision recorded as an OPEN QUESTION in the discoverability record"
  ],
  "rationale_ref": "spec/productization-work-orders/P18-live-ux-action-wiring.md",
  "work_order": "P18-C"
}

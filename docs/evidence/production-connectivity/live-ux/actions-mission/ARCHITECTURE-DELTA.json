{
  "affected_artifacts": [
    "apps/web/app/live-mission",
    "apps/web/app/mission",
    "apps/web/app/api/live-mission",
    "apps/web/live-mission",
    "tests/live-ux/actions",
    "docs/evidence/production-connectivity/live-ux/actions-mission"
  ],
  "added": [
    "apps/web/app/live-mission/data-seam.ts: THE live-mission data seam (the architect-defined binding for lanes A and B — neither lane references the other's files): exports getLiveMissionData(): Promise<LiveObservationData> whose P18-B default body returns the HONEST empty state emptyLiveObservation('unwired','never','github:repo:payswapdotorg/SOS-2.0') (imported read-only from the P17-C DTO). The mission routes consume ONLY this function; the architect's A→B→C integration pass swaps the seam body for lane P18-A's createLiveMissionDataProducer (apps/web/live-data/src/producer.ts) — one function, one file.",
    "apps/web/app/live-mission/page.tsx: the route mount (MOUNTING.md integration step 1, near-verbatim): mounts the P17-C LiveMissionPage over the data seam, force-dynamic (live state is never statically cached).",
    "apps/web/app/mission/page.tsx + mission-live-route.tsx: the REPLACED production Mission route (the operator's structural fix) — the read-only fixture-backed mission view gives way to the LIVE Mission experience (Start mission / Import system / Resume existing mission + live observation + authority-gated consequential actions). The route wrapper stays thin (the frozen P1 shell pin: ≤ 11 lines, shell-referencing) and delegates to the route body module; the explanation strengths of the read-only view are preserved by the live surface itself: the six product review questions are answered inline on every live card (the rationale presentation, kept current with observed state instead of a fixture revision), retained uncertainty stays visible (honest UNKNOWN/NO_DATA + ALIGNED/DIVERGED/UNVERIFIED/STALE findings), and every consequential action receipt renders evidence and rationale deep links (/evidence, /rationale).",
    "apps/web/app/mission/receipt/page.tsx: the POST-redirect-GET receipt page — the browser path for consequential actions: reads the IN-PROCESS receipt ledger (idempotencyKey -> receipt view) or the submission's own key-validated receipt round-trip cookie (the serverless multi-instance reality; the cookie is server-minted, HttpOnly, SameSite=Lax, budget-capped, and the page accepts it only when its idempotency key MATCHES the URL key — never an injection surface), renders the receipt inside the app shell, and states the honest not-in-this-process scope for unknown keys (never fabricated from the key alone).",
    "apps/web/app/api/live-mission/actions/**: LIVE_ACTION_ENDPOINT = /api/live-mission/actions (MOUNTING.md integration step 2). live-action-core.ts is the whole submission pipeline with ZERO ambient boundaries (no network/time/env — all injected, so the deterministic suite drives the REAL pipeline): parse (form-encoded `action` field or JSON body) -> dispatch by envelope shape (family -> gateway action; entryId+resolution -> ASK resolution) -> deterministic completion of the mounted forms' partial envelopes (actionId + idempotencyKey as content addresses of the envelope's semantic content — same form -> same key, always; full P17-C-builder envelopes honored as carried) -> the merged ActionGateway execution (typed validation incl. AUTHORITY_FIELD_SMUGGLED; the idempotency claim; CURRENT authority re-evaluation at action time — fail-closed; typed executor operations; evidence + event emission bound to the exact revisions acted on) -> typed receipt views (gateway-action / ask-resolution / malformed, all serializable, every one carrying its honesty notes: authority-at-action-time, the in-process idempotency scope with the durable adapters UNAVAILABLE, the replay marker). ASK envelopes resolve through the merged AskQueue.resolve() (human resolution authority) with an endpoint-level idempotency wrapper (the same resolution envelope replays the recorded outcome; the queue itself is terminal per entry) and TYPED honest failure mapping (ASK_ENTRY_UNKNOWN for the unwired ask plane — never a fabricated resolution; ASK_ALREADY_RESOLVED; ASK_RESOLUTION_INVALID). deployed-host.ts binds the process boundary: an env-configured AuthorityPort (SOS_LIVE_MISSION_GRANTS 'actor|family|scope' entries, RE-READ on every evaluation — removal revokes at action time; absent = GRANT_NEVER_HELD = fail-closed), the real provider executors behind the sync bridge when credentials exist, an honestly EMPTY AskQueue, and a globalThis-keyed host singleton + receipt ledger (Next bundles the route and the page as separate entries). real-executor-bridge.ts binds REAL async providers behind the FROZEN synchronous executor seam (the P8 discipline — an adapter, not a contract change): a worker thread owns every real HTTP round-trip (OpenRouter body.start — a real chat completion as the body's first breath, carrying provider+model+state RUNNING; Vercel promotion/rollback/deployment — real POST /v13/deployments git deployments + the native instant rollback with a git restore-deployment fallback; GitHub revision binding — a read-only commit-existence check inside the executor, after the authority grant) while the sync execute()/verify() calls block on a SharedArrayBuffer until the worker writes the JSON result — every failure (timeout, unconfigured provider, provider refusal with the verbatim HTTP status + error envelope) is a TYPED honest failure, never a fabricated provider outcome. route.ts is the thin adapter: reads the ambient environment exactly once per process (env-only; values never echoed), binds the deployed host with the boundary clock, and answers honestly — a browser form POST gets 303 See Other to /mission/receipt?key=<idempotencyKey> with the receipt cookie; a programmatic submission (Accept: application/json) gets the typed receipt JSON; malformed submissions get the typed 400.",
    "apps/web/live-mission/src/components/action-receipt-view.tsx: the receipt rendering wiring (an integration seam ONLY — the P17-C components/view-state/envelopes stay semantically frozen; a NEW file, gateway-free, consuming the endpoint's receipt views through STRUCTURAL type mirrors — the live-mission-dto precedent, no runtime edge): renders the status (SUCCEEDED/FAILED/DENIED/RESOLVED), the authority decision (re-evaluated at action time, reason, grant id), the DENIED safe-failure UX (nothing executed, the designed behavior, not an outage), the typed failure with retryability, the executor output (what actually happened), the rollback verification block (VERIFIED/FAILED/UNKNOWN — UNKNOWN stays UNKNOWN), the evidence ids + /evidence and /rationale deep links (the receipt never stands alone), the six-question review block (the rationale presentation), the idempotency scope + replay marker, and the ask receipt variant (the decision record as the evidence binding; the honest ASK failures).",
    "tests/live-ux/actions (@sos-2/tests-live-ux-actions): the acceptance suites, STRICTLY SEPARATED. (1) the DETERMINISTIC REFERENCE-MODE SUITE (default; offline; fixed seed 424242; 88 tests): envelope validation + typed rejections (smuggled authority fields named; malformed fields; form/JSON parsing), deterministic envelope completion (same form -> same derived key; different content -> different key; carried envelopes honored), authority-at-action-time enforcement through the REAL merged gateway (held executes with evidence; revoked/expired/never-held/wrong-scope fail CLOSED with the executor NEVER invoked; the env-configured authority re-read per evaluation), idempotency (double-submit returns the recorded receipt, exactly one world transition, one action.succeeded event; denied receipts replay too; ASK replay; the in-process receipt ledger + the receipt cookie compact/parse/key-validation + budget), receipt rendering (every receipt shape server-rendered: status, authority, safe-failure/denied UX, evidence ids + deep links, six questions, idempotency scope honesty, rollback verification verdict, ASK receipts, JSON round-trips), the ASK resolution contract (pending resolution through the real queue minting the Decision record; terminal entries; unknown entries typed; invalid shapes typed), the mount seams (the data seam returns the honest 'unwired' state and is deterministic; the REPLACED /mission route renders the live experience server-side — entry points, LIVE badges, honest empty states, the action forms POSTing to /api/live-mission/actions with honestly disabled buttons until a head is observed, first-class onboarding, the six questions, the honest Packages link; the /live-mission route mounts identically; both routes force-dynamic; the receipt page renders the honest not-in-this-process state), and redaction/structure (no credential values in any owned file — the merged corpus with zero findings, env NAMES only; no credential echo into receipts; process.env ONLY in the route handler; ambient time only there; network only inside the bridge worker boundary; no client components; the worker bridge fails with TYPED honest failures offline; pnpm-workspace.yaml gained only the tests/live-ux/actions importer entry). (2) the ENV-GATED REAL INTEGRATION SUITE (RUN_REAL=1, default OFF, gated by scripts/run-real-if-env.mjs): the deployed journey — provider probes (honest states), the Vercel project env binding (env-only credential NAMES as encrypted project env vars + the lane-scoped binding names), the EXACT-branch-head deployment through RealVercelDeploymentProvider (gitSource ref = the head sha; binding verified; retries for the fresh-push GitHub index propagation; LIVE_MISSION_BASE_URL mode re-verifies an existing deployment of the branch), deployed-surface probes (the live mission experience LIVE over real HTTP; the endpoint answers 405 on GET), REAL consequential actions against the DEPLOYED preview URL (body summon with a REAL OpenRouter round-trip inside the executor seam after the action-time grant; idempotent replay; a REAL denied receipt for a never-held grant; promotion/rollback through the deployment provider — real provider records when the provider answers, typed honest refusal receipts when it does not; ASK resolution honest failures + the real queue contract locally), all recorded as redacted HTTP transcripts + typed receipts + honest provider states + the deployment record (P3 registrar shape with the rollback pointer) + the journey log + the secrets audit.",
    "docs/evidence/production-connectivity/live-ux/actions-mission: the machine-readable evidence records — the action receipts transcripts (real outcomes including the provider refusals, verbatim), the deployment record, the journey log, the secrets audit (38 files, 0 findings) and this README (reproduction commands with env NAMES only)."
  ],
  "removed": [],
  "modified": [
    "apps/web/app/mission/page.tsx (the base read-only wrapper): REPLACED by the live experience mount (the operator's structural fix) — the shell's MissionPage module itself is untouched and remains for the frozen shell suite; only this route's wrapper changed (still thin, still shell-referencing, per the P1 navigation pin).",
    "apps/web/package.json (the app skeleton): ONLY the dependencies @sos-2/action-gateway and @sos-2/ask (workspace:*) — the mechanical importer addition MOUNTING.md step 2 requires (the endpoint executes through the merged gateway + ask queue INSIDE the web app; the Vercel build command's dependency closure builds them automatically). No script, build or dev change; apps/web's own frozen test script is untouched (the live-mission render suite keeps its P17-C wiring).",
    "pnpm-workspace.yaml: ONE importer entry (- tests/live-ux/actions) with the lane comment — mechanical, test-only wiring; the string 'live-mission' appears nowhere (the P17-C structure pin holds).",
    "pnpm-lock.yaml: the mechanical importer additions (apps/web's two new workspace deps + the new tests/live-ux/actions package)."
  ],
  "preserved_invariants": [
    "the frozen W0-W18 core and every merged P0-P17 file remain untouched: all changes live under the owned paths apps/web/app/live-mission, apps/web/app/mission, apps/web/app/api/live-mission, apps/web/live-mission/src/components/action-receipt-view.tsx (a NEW integration-seam file), tests/live-ux/actions and docs/evidence/production-connectivity/live-ux/actions-mission; the P17-C components/view-state/envelopes are byte-frozen (the receipt view is a new file consuming them read-only through structural mirrors); spec/, scripts/, .github/, apps/web/shell, apps/web/onboarding, apps/web/ecology and every merged package are unmodified",
    "authority is re-evaluated AT ACTION TIME, fail-closed: the merged ActionGateway performs exactly ONE evaluation per submission; a revoked, expired, never-held or wrong-scope grant DENIES with the executor NEVER invoked (pinned with the counting-executor assertions + the real denied receipt from the deployed endpoint); the env-configured authority binding re-reads the CURRENT grant list on every evaluation",
    "idempotency: the same typed envelope maps to the same idempotency key (deterministically derived for the forms' partial envelopes; carried for full envelopes) and replays the recorded original receipt — exactly one world transition, one action.succeeded event, one OpenRouter round-trip (REAL: the deployed endpoint answered replayed); the in-process scope is stated on every receipt — the durable adapters are UNAVAILABLE, so NO durable confirmation is claimed anywhere",
    "the UI never mutates state directly: every consequential action is the P17-C typed envelope POSTed to /api/live-mission/actions, validated and executed through the merged gateway; package composition stays honestly linked to the Packages workspace (no fabricated action family); the ASK resolution authority is the HUMAN resolver through the merged queue",
    "honest states are never fabricated: every provider state is machine-checkable (CONNECTED/UNKNOWN/UNAVAILABLE/DEGRADED) and derives from real records; provider refusals are typed honest failures carrying the verbatim HTTP status + error envelope (the deployed promotion/rollback receipts record the REAL 403 function-context refusal); UNKNOWN stays UNKNOWN (the rollback verifier); the ask plane answers its honest unwired failure; the receipt page never fabricates an outcome from a key alone",
    "server-rendering discipline holds: no client components, no client fetches — live data enters through server-produced props (the data seam); the receipts render server-side (the PRG hop + the key-validated receipt cookie — server-minted browser round-trip, not a client fetch); network happens ONLY inside the bridge worker behind the executor seam (after the authority grant), never in any render path; process.env only in the route handler; ambient time only at that boundary",
    "demo/live honesty: the mission surface carries LIVE provenance (never a DEMO badge on its own cards); the honest 'unwired' seam state renders UNKNOWN/NO_DATA until lane P18-A lands (never fabricated live state); receipts carry the evidence ids + /evidence and /rationale deep links and the six-question rationale block",
    "secret discipline (env-only; names never values): no credential value appears in any source, test or evidence file — the lane secret scan (both merged corpora) reports 0 findings over 38 owned files (the two synthetic-fixture suites excluded and clearly marked); the evidence writer fail-closes on residual secret-shaped material; transcripts and receipts pass through the dual redaction corpora before any file is written",
    "deterministic reference-mode tests remain green and strictly separated from the real-integration suite: pnpm -r --if-present test runs ONLY the deterministic suites (104 test-file groups, 4038 tests, run-to-run identical with the fixed seed); RUN_REAL=1 gates the real suite (default OFF) which records honest outcomes including provider refusals",
    "no frozen-contract change and no sibling-lane dependency: the merged packages are consumed read-only through their public exports; the frozen SYNCHRONOUS ActionExecutor seam is bound to real async providers through a worker-thread sync bridge (an ADAPTER behind the seam — the P8 'real providers attach later as adapters' discipline; the gateway/ask/authority contracts are byte-frozen); lane P18-A's files (apps/web/live-data) and lane P18-C's files are never referenced; the seam decouples lane B completely"
  ],
  "boundary_changes": [
    "four new owned locations under apps/web: the route mount (app/live-mission: page.tsx + data-seam.ts — the architect-defined one-function seam), the REPLACED mission route (app/mission: the thin page.tsx wrapper + mission-live-route.tsx + receipt/page.tsx), the action endpoint (app/api/live-mission/actions: route.ts + live-action-core.ts + deployed-host.ts + real-executor-bridge.ts) and the receipt rendering seam (live-mission/src/components/action-receipt-view.tsx — a NEW file in the P17-C module directory, gateway-free, structural mirrors only; the P17-C sources are byte-frozen and their import discipline pin still passes)",
    "dependency edges: apps/web -> @sos-2/action-gateway + @sos-2/ask (the mechanical importer addition the endpoint REQUIRES — MOUNTING.md step 2 mandates the merged gateway + ask queue inside the web app; the Vercel closure build command builds them automatically); tests/live-ux/actions -> the merged action-gateway/ask/authority/decision/deployment-providers/real-observation packages (the P18-C test-package precedent; test-time aliases to the app sources create no package edges); ONE pnpm-workspace.yaml importer entry (- tests/live-ux/actions, never 'live-mission' — the P17-C pin holds); the lockfile change is importer addition only",
    "the deployed runtime binding is env-only on the Vercel project (encrypted credentials; SOS_LIVE_MISSION_GRANTS as the authority configuration; the lane-scoped SOS_LIVE_MISSION_VERCEL_TOKEN/_PROJECT_ID names are preferred after the VERCEL_TOKEN function-context 403 observation — an OPEN QUESTION for the architect: the same token answers 200 (reads) and 402 (creation quota) from outside while the function context answers 403; the account deployment quota (100/day) was exhausted during the run — reset 2026-09-27T15:51Z; the promotion/rollback real journeys rerun cleanly after the reset)",
    "THE ARCHITECT'S INTEGRATION POINT (A→B→C): swap the data-seam body for lane P18-A's createLiveMissionDataProducer (one function, one file); the ask plane producer lands with the decision plane (the deployed queue is honestly empty until then); the mission store for resumable missions lands with the mission plane; the durable idempotency/receipt adapters land when the persistence providers answer (in-process scope stated honestly meanwhile)"
  ],
  "rationale_ref": "spec/productization-work-orders/P18-live-ux-action-wiring.md",
  "work_order": "P18-B"
}

{
  "schema_version": "1.0",
  "lane": "B",
  "work_order": "P15",
  "journey_id": "p15b-interruption-replacement-resume",
  "journey": "body interruption -> replacement -> resume (a leased body is killed mid-task; a replacement is summoned; the task resumes from durable checkpoints WITHOUT losing semantic identity)",
  "scenario": {
    "package": "@sos-2/tests-autonomous-build",
    "test_file": "tests/autonomous-build/test/interruption-replacement.test.ts",
    "test_name": "killing the body mid-task loses NOTHING durable; the replacement resumes from the verified checkpoint"
  },
  "revisions": {
    "base": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
    "implementation_head": "0000000000000000000000000000000000000000",
    "head_note": "implementation_head is the lane-branch commit containing the suite sources and these evidence records; the finalization follow-up commit (evidence-only) records it. The architect gate binds to the branch head."
  },
  "terminal_state": {
    "interruption": "KILL at step index 5 (2 durable checkpoints captured: cp-0001, cp-0002)",
    "lease_verdict_sequence": ["HEALTHY", "SUSPECTED (truthful UNKNOWN)", "LOST (verified failure only)"],
    "replacement": "REPLACED (a different body, a fresh lease, the old lease ended)",
    "resume": "runProgram fromCheckpoint cp-0002 — 7 remaining outcomes (steps 4..10), continuity not a restart",
    "completion": "COMPLETED with final_verification.verified (independent verifier over prior + resumed evidence)"
  },
  "evidence_graph_links": [
    "the durable task record survives the kill: identity (task_id, mission_ref, authority_context.grant_refs) and the accumulated checkpoints stand; the record revision only moves forward",
    "the recovery plan inherits the task identity + the LAST durable checkpoint (cp-0002) + the authority context, and names exactly 3 unknown-until-reverified facts (in-flight side effects reconciled, body-local session state rebuilt, the failed body's post-failure truth UNKNOWN until re-observed)",
    "the replacement body is a DIFFERENT registered cloud body on a fresh lease; the lost lease is ended; the graph node keeps mission_ref + authority_ref across the replacement",
    "the accumulated evidence survives: prior checkpointed outputs (steps 0..3) + the resumed segment (steps 4..10) == the full 11-step program, step-for-step — the completion record is verified over the COMBINED evidence",
    "the timeline replays the whole journey in order: heartbeat (lease.suspected with truthful UNKNOWN, then lease.lost) -> recovery (task.body-replaced) -> checkpoint -> completion"
  ],
  "retained_uncertainty": [
    "the verified failure is a scripted provider attestation (ManualFailureVerification seam) — a real provider's post-incident record attaches behind the same port",
    "a missed beat alone NEVER becomes LOST: the SUSPECTED verdict is truthful UNKNOWN and stays SUSPECTED without verification (pinned by the provider-outage record too)",
    "the recovery plan explicitly retains what is unknown until re-verified (in-flight side effects, body-local state, the failed body's post-failure truth)"
  ],
  "simulated_windows": {
    "outage_windows": [],
    "interruptions": [
      "KILL interrupt at work-program step index 5 (the injected body loss seam of the deterministic runProgram engine)"
    ],
    "user_offline": false
  },
  "reproducibility": {
    "seed": 424242,
    "command": "pnpm --filter @sos-2/tests-autonomous-build test",
    "runs_identical": true
  }
}

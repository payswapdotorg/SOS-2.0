{
  "schema_version": "1.0",
  "lane": "B",
  "work_order": "P15",
  "journey_id": "p15b-flagship-autonomous-build",
  "journey": "empty GitHub repository -> implementation (the P13 §11 flagship greenfield journey, driven end-to-end autonomously: mission -> connected repo (reference adapter) -> authority approved -> pipeline -> task graph -> cloud bodies (reference) -> implementation -> independent evaluation -> commit/PR/push (reference executors) -> deployment (reference) -> runtime verification -> evidence-backed completion — with NO user tick after kick-off)",
  "scenario": {
    "package": "@sos-2/tests-autonomous-build",
    "test_file": "tests/autonomous-build/test/flagship-journey.test.ts",
    "test_name": "completes the full §11 chain with NO user-online tick after kick-off (device-optional)"
  },
  "revisions": {
    "base": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
    "implementation_head": "0000000000000000000000000000000000000000",
    "head_note": "implementation_head is the lane-branch commit containing the suite sources and these evidence records; the finalization follow-up commit (evidence-only) records it. The architect gate binds to the branch head."
  },
  "terminal_state": {
    "stage": "COMPLETED",
    "status": "COMPLETED",
    "pending_ask": null,
    "completion_id_reproduces": true,
    "second_run_identical": true
  },
  "evidence_graph_links": [
    "report.sourceRevisions.workspaceHead === gatewayWorld.heads.get('workspace') (the workspace commit chain reached the reported head; imported base repository acme/p15b-empty-repo)",
    "report.sourceRevisions.commits task ids === the reference plan's components (scaffold + one per mission goal), each commit sha content-addressed (source:*)",
    "gatewayWorld.pushes === [{ remote: github.com/acme/p15b-empty-repo, ref: sos/mission-flagship-p15b-01, sha: workspaceHead }]",
    "gatewayWorld.pullRequests[0] headBranch sos/mission-flagship-p15b-01 -> baseBranch main, recorded in report.sourceRevisions.pullRequest",
    "gatewayWorld.deployments.get(deploymentId) === { environment: production, sourceSha: workspaceHead, status: ACTIVE }",
    "report.evaluation.verdictRefs every targetSourceRevision === workspaceHead; passed === requiredTypes.size (the plan's assurance constraints), failed 0, unknown 0",
    "every consequential action (commit/push/pull-request/deployment families) succeeded through the P9 action gateway with action-time authority re-evaluation (every evaluation GRANTED) and evidence emitted per action",
    "every task node carries mission_ref + authority_ref and a verified final_verification; the task + evidence timeline replays (journey stage events in §11 order, graph-obs/fabric-obs events, the body's non-authoritative completion report retained in the durable trace)"
  ],
  "retained_uncertainty": [
    "all reference surfaces are SIMULATED (reference github adapter, in-memory git host, in-memory deployment target, CloudCodingShellBody simulators, scripted reference probes) — no real GitHub/git/deployment/provider evidence exists in this dogfood",
    "the completion report retains its reference-mode uncertainty entries verbatim (remainingUncertainty includes SIMULATED and 'reference mode' markers) and honest provider statuses ride along",
    "the mission statement is formalized by the deterministic reference formalizer; a connected reasoning provider may refine it later (non-authoritative assumption retained on the mission)"
  ],
  "simulated_windows": {
    "outage_windows": [],
    "interruptions": [],
    "user_offline": true
  },
  "reproducibility": {
    "seed": 424242,
    "command": "pnpm --filter @sos-2/tests-autonomous-build test",
    "runs_identical": true
  },
  "independence_pin": {
    "test_file": "tests/autonomous-build/test/flagship-journey.test.ts",
    "test_name": "the acting body CANNOT certify its own output: typed independence denial, the probe never runs"
  }
}

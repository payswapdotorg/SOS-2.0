{
  "journey_id": "package-composition",
  "test_id": "tests/product-dogfood/test/package-composition.test.ts::P15 Lane A — package composition: the ecology updates as DATA (learned records)",
  "base_sha": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
  "head_sha": "a7c090bc76cbd0dbb730850bca82e20cd8e7994e",
  "terminal_state": "ECOLOGY_UPDATED_AS_DATA (the spine+store COMPATIBLE_WITH assertion is a learned record in the EcologyGraph — never a package internals mutation)",
  "evidence_graph_links": [
    "spine package envelope id (the W6 golden fixture — sos://Package/<32 hex>)",
    "store package envelope id (the P15a-created package — sos://Package/<32 hex>, content-addressed)",
    "composition envelope id (sos://PackageComposition/<32 hex>, content-addressed)",
    "composition content.members[].package_id (both packages referenced as members)",
    "ecology assertion id (deterministic content-addressed, 64 lowercase hex — NOT a spine artifact id)",
    "ecology assertion evidence_refs (the composition's evidence ref — the typed trace link)",
    "ecology assertion provenance (the P15a:package-composition:ecology-assertion entry)",
    "packageArtifactId(input) === the created artifact's envelope id (the deterministic derivation)",
    "compositionArtifactId(input) === the created artifact's envelope id (the deterministic derivation)"
  ],
  "retained_uncertainty": [
    "remaining uncertainty: applicability estimates are QUALITATIVE + UNQUANTIFIED (never a fabricated quantitative score)",
    "remaining uncertainty: maturity is DISCOVERED for the store package and FORMING for the composition (never a fabricated VALIDATED)",
    "remaining uncertainty: the diversity profile is retained (the family + the dimensions/stances — never collapsed)",
    "remaining uncertainty: the assurance obligations are typed (TEST + REPLAY — never bypassed)"
  ],
  "reproducibility": {
    "seed": 424242,
    "command": "pnpm --filter @sos-2/tests-product-dogfood exec vitest run test/package-composition.test.ts",
    "deterministic": true,
    "bit_exact_reproduction": true
  }
}

{
  "scenario_id": "negative-revoked-authority",
  "work_order": "P15",
  "lane": "C",
  "base_revision": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
  "verified_head_revision": "PENDING_EXACT_HEAD_SHA (filled by the evidence follow-up commit: the exact code-head sha the gates ran against; the follow-up adds only these evidence records)",
  "test_file": "tests/accessibility/test/06-revoked-authority.test.ts",
  "test_names": [
    "CONTAINED (journey): a mid-journey revocation parks the flagship journey behind a typed AUTHORITY_REQUIRED ask — nothing consequential runs",
    "CONTAINED (journey): after the revocation the evidence graph stays queryable and truthful",
    "CONTAINED (gateway): a revoked grant fails closed — GRANT_REVOKED, executor NEVER invoked, denial durable",
    "CONTAINED (gateway): revocation wipes EVERY family — no privilege survives its authority"
  ],
  "terminal_state": "FAIL-CLOSED: journey parks AWAITING_ASK (REALIZATION / AUTHORITY_REQUIRED) with zero commits and completionReport null; gateway denies every family with GRANT_REVOKED",
  "detection_containment": [
    "the typed ask is parked first-class and visible as journey.ask-parked in the user-visible timeline",
    "the denial is durable evidence (action.denied in the event log; every action-time evaluation returns granted=false)"
  ],
  "retained_uncertainty": [],
  "reproducibility": {
    "seed": 424242,
    "command": "corepack pnpm install --prefer-offline && pnpm --filter @sos-2/tests-accessibility test"
  },
  "notes": "The suites ran twice at the verified head (identical results) and re-ran green at the delivery head (the evidence-only follow-up commit that fills verified_head_revision with the exact code-head sha and adds only these records). Fixed vitest seed 424242; fully offline (every clock, store, seam and probe injected)."
}

{
  "scenario_id": "negative-secret-leakage",
  "work_order": "P15",
  "lane": "C",
  "base_revision": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
  "verified_head_revision": "f9d409ad57ed69ad8c4b11741d7f9784496b27e9",
  "test_file": "tests/accessibility/test/14-secret-leakage.test.ts",
  "test_names": [
    "CONTAINED (REDACT_AND_PASS): a secret-shaped emission is redacted AND the redaction is observed (audited)",
    "CONTAINED (DENY): the leaked content never reaches the artifact OR the outcome record",
    "DETECTED: the scanner recognizes the synthetic secret shapes (the detection itself is exercised)",
    "SECRETS-BY-REFERENCE (product surface): a full flagship journey produces ZERO secret-shaped values in every record a user can see"
  ],
  "terminal_state": "CONTAINED: redaction (audited) or typed denial at the observation boundary; a whole-journey scan of every user-visible record pool finds zero secret-shaped values",
  "detection_containment": [
    "inspectEmission: PASSED_REDACTED with the secret gone from the content + the REDACT audit fact; DENIED_SECRET_LEAK whose outcome record never echoes the value",
    "secrets-by-reference at the product surface: the sandbox policy names secrets BY NAME; scans over gateway evidence, action events, observation events, the completion report and the durable task records find zero secret shapes"
  ],
  "retained_uncertainty": [],
  "reproducibility": {
    "seed": 424242,
    "command": "corepack pnpm install --prefer-offline && pnpm --filter @sos-2/tests-accessibility test"
  },
  "notes": "The suites ran twice at the verified head (identical results) and re-ran green at the delivery head (the evidence-only follow-up commit that fills verified_head_revision with the exact code-head sha and adds only these records). Fixed vitest seed 424242; fully offline (every clock, store, seam and probe injected)."
}

{
  "scenario_id": "negative-expired-authority",
  "work_order": "P15",
  "lane": "C",
  "base_revision": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
  "verified_head_revision": "f9d409ad57ed69ad8c4b11741d7f9784496b27e9",
  "test_file": "tests/accessibility/test/05-expired-authority.test.ts",
  "test_names": [
    "CONTAINED: an EXPIRED credential fails closed at action time — DENIED receipt, executor NEVER invoked",
    "CONTAINED: expiry crossing DURING the journey still fails closed at action time (exactly one re-evaluation per action)",
    "CONTAINED: an expired authority grant NEVER authorizes promotion (confidence is not authorization)",
    "the expired-authority denial is RECORDED (audit trail) and the evidence graph stays truthful"
  ],
  "terminal_state": "FAIL-CLOSED: DENIED receipt (ACTION_AUTHORITY_DENIED / GRANT_EXPIRED), executor never invoked, action.denied durable, promotion REJECT on the expired grant",
  "detection_containment": [
    "action-time re-evaluation: exactly one evaluateCurrent per action; the denial names GRANT_EXPIRED",
    "the promotion gate authority evaluation marks the grant EXPIRED at the decision instant (a STRONG confidence mark does not help)",
    "the DENY decision lands in the InMemoryAuditTrail; every emitted evidence record carries the DENIED truth"
  ],
  "retained_uncertainty": [],
  "reproducibility": {
    "seed": 424242,
    "command": "corepack pnpm install --prefer-offline && pnpm --filter @sos-2/tests-accessibility test"
  },
  "notes": "The suites ran twice at the verified head (identical results) and re-ran green at the delivery head (the evidence-only follow-up commit that fills verified_head_revision with the exact code-head sha and adds only these records). Fixed vitest seed 424242; fully offline (every clock, store, seam and probe injected)."
}

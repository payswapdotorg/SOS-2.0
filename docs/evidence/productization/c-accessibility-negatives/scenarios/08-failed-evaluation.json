{
  "scenario_id": "negative-failed-evaluation",
  "work_order": "P15",
  "lane": "C",
  "base_revision": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
  "verified_head_revision": "f9d409ad57ed69ad8c4b11741d7f9784496b27e9",
  "test_file": "tests/accessibility/test/08-failed-evaluation.test.ts",
  "test_names": [
    "CONTAINED: a failing evaluation denies completion, drives bounded repair, then parks behind the typed repair-exhausted ask (never a fabricated pass)",
    "CONTAINED: with NO connected runtime verifier the verdict is honestly UNKNOWN — completion is refused, an ask is parked (never a fabricated pass)",
    "after the failed evaluations the evidence graph stays queryable and truthful (both fault worlds)"
  ],
  "terminal_state": "DENIED: completion refused in both fault modes — AWAITING_ASK (COMPLETION / EVALUATION_REPAIR_EXHAUSTED) after exactly the bounded 2 repair attempts; AWAITING_ASK (COMPLETION / RUNTIME_VERIFICATION_NOT_PASSED) with the honest UNKNOWN verdict",
  "detection_containment": [
    "completionReport() is null in both worlds (no fabricated completion from failing or absent evidence)",
    "the durable graph parks the node AWAITING_INPUT with the ask overlay; the ask is visible as journey.ask-parked",
    "the repair loop is bounded (exactly 2 repair commits — never an infinite loop)"
  ],
  "retained_uncertainty": [
    "the default-registry world keeps the runtime verification honestly UNKNOWN (NOT_YET_CONNECTED probes are never treated as passes)"
  ],
  "reproducibility": {
    "seed": 424242,
    "command": "corepack pnpm install --prefer-offline && pnpm --filter @sos-2/tests-accessibility test"
  },
  "notes": "The suites ran twice at the verified head (identical results) and re-ran green at the delivery head (the evidence-only follow-up commit that fills verified_head_revision with the exact code-head sha and adds only these records). Fixed vitest seed 424242; fully offline (every clock, store, seam and probe injected)."
}

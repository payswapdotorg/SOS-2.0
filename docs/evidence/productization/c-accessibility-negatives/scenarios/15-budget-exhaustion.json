{
  "scenario_id": "negative-budget-exhaustion",
  "work_order": "P15",
  "lane": "C",
  "base_revision": "711cfd60b7eb90b338a8ce17cadcafb72a0d1e6e",
  "verified_head_revision": "PENDING_EXACT_HEAD_SHA (filled by the evidence follow-up commit: the exact code-head sha the gates ran against; the follow-up adds only these evidence records)",
  "test_file": "tests/accessibility/test/15-budget-exhaustion.test.ts",
  "test_names": [
    "CONTAINED: an exhausted concurrency budget is a typed BUDGET_EXCEEDED denial (no 11th-hour fabrication)",
    "CONTAINED: exhausted COST UNITS and DURATION are typed denials too (every counter fails closed)",
    "HONEST: incomplete accounting is ACCOUNTING_UNKNOWN — never a silently guessed allowance",
    "RECORDED: the exhaustion denial lands in the policy audit trail (the evidence of the stop)",
    "NO FABRICATED COMPLETION: the task a budget denial stops stays truthfully un-completed (durable state, cost evidence)",
    "the CONTROL: a task inside every limit is allowed (bounded execution works, not just denies)"
  ],
  "terminal_state": "HONEST DEGRADATION: typed BUDGET_EXCEEDED for every exhausted counter; ACCOUNTING_UNKNOWN for incomplete accounting; the denial audited; the cost ledger evidence-only; the stopped task never fabricated completed",
  "detection_containment": [
    "evaluateBudget names the counter + limit in the typed denial; incomplete costUnits accounting yields ACCOUNTING_UNKNOWN (never a guessed allowance)",
    "the DENY decision lands in the policy audit trail (surface=budget)",
    "the durable task record keeps its truthful non-COMPLETED status with final_verification null while the cost ledger appends the typed episode"
  ],
  "retained_uncertainty": [
    "ACCOUNTING_UNKNOWN retains the honest statement that an incomplete accounting never becomes a guessed allowance"
  ],
  "reproducibility": {
    "seed": 424242,
    "command": "corepack pnpm install --prefer-offline && pnpm --filter @sos-2/tests-accessibility test"
  },
  "notes": "The suites ran twice at the verified head (identical results) and re-ran green at the delivery head (the evidence-only follow-up commit that fills verified_head_revision with the exact code-head sha and adds only these records). Fixed vitest seed 424242; fully offline (every clock, store, seam and probe injected)."
}

{
  "affected_artifacts": [
    "tests/accessibility (@sos-2/tests-accessibility): the fresh-user flagship acceptance suite (the P15 headline — the complete §11 journey through user-reachable surfaces only: the composed product composition over the merged public exports + the P11 local-companion reference runtime; progress visible as typed records with evidence, never raw agent transcripts; evidence-gated completion; exact-revision links; retained plain-terms uncertainty) + the 15 negative-case suites (one per Work-Order bullet, the W17 discipline: each fault CONSTRUCTED, DETECTED/CONTAINED with a typed record, never a silent pass, never a crash, the evidence graph queryable and truthful afterward) + the structural discipline suite (zero external dependencies, offline determinism, import boundary, fixed seed, lockfile identity, the evidence-lane shape and this delta's schema validation)",
    "docs/evidence/productization/c-accessibility-negatives: the lane evidence record — one machine-readable JSON per scenario (the flagship journey + every negative bullet: exact base/verified-head revisions, test file + test names, terminal state, detection/containment record links, retained uncertainty, reproducibility seed + command) + this Architecture Delta record"
  ],
  "added": [
    "tests/accessibility/test/00-flagship-fresh-user.test.ts: the fresh-user acceptance — a plain-language mission completes the full flagship journey with the user device offline on every cloud tick; every user-visible record is a typed record with evidence (stage timeline + action evidence chain + the body's non-authoritative report kept distinct from the independent certification); the repository, runtime and deployment are linked to EXACT revisions (workspace head, per-task commits, push, pull request, deployment, imported base — cross-checked against the reference git world, never decorative); all unresolved uncertainty is retained in plain, non-developer-encoding terms; the six product-review questions (what/why/evidence/uncertainty/authority/next) are each answerable from the user-visible record; the completion record reproduces bit-exactly across identical journeys; the optional P11 local-companion leg queues a local work order while the device is offline and completes verified on reconnect with the honest NOT_YET_INSTALLED installation status",
    "tests/accessibility/test/01-stale-state.test.ts .. 15-budget-exhaustion.test.ts: one negative-case suite per Work-Order bullet — stale state (superseded revision detected, promotion gate REJECTS, evidence classified SUPERSEDED), missing evidence (typed EVIDENCE_MISSING invalidation, fail-safe verdict INVALID), contradictory evidence (CONTRADICTED_BY_EVIDENCE surfaced, never averaged away), unknown/unavailable telemetry (bit-exact UNAVAILABLE truth states through observation ingestion, replay protection, provider health UNKNOWN without probes, outage windows truthful), expired authority (action-time GRANT_EXPIRED fail-closed with the executor never invoked, expiry crossing mid-lifetime still denied, expired grants never authorize promotion), revoked authority (journey parks behind the typed AUTHORITY_REQUIRED ask with zero commits, gateway GRANT_REVOKED denial durable, no privilege survives revocation), unsafe candidate (REFUTED/EXPIRED/INCOMPLETE assurance never promotes — REJECT/GATHER_EVIDENCE, and a defective body output is failed by the independent evaluation and never becomes the final revision), failed evaluation (completion denied, bounded repair then the typed repair-exhausted ask, honest UNKNOWN with the default registry — never a fabricated pass), body crash (the kill visible as typed durable events, SUSPECTED never LOST without verified failure, the recovery plan retains unknown-until-reverified, no fabricated completion), task resume from checkpoint (identical completion evidence to the uninterrupted run, the resume visible as timeline continuity, uncertainty statement retained, corrupted checkpoints fail CLOSED as CHECKPOINT_INVALID), failed rollback (typed lifecycle errors for never-deployed/terminal rollbacks with truthful retained state + queryable transitions, executor-failed rollbacks durable as action.failed, unobservable rolled-back states honestly UNKNOWN), demo/live-state confusion (demo dataset permanently typed with the demo-fixture provenance marker bound into content-derived identity, malformed/forged data sources rejected with typed WebContractError, LIVE sources require the exact durable-store field set, ZERO demo markers or demo ids in the live evidence graph of a completed journey), cross-project access (typed ISOLATION denial fail-closed for every operation, the denial recorded in the policy audit trail and attributed by diagnostics, same-project access still allowed), secret leakage (REDACT_AND_PASS redacts + audits the redaction, DENY blocks with the secret never reaching the outcome, secrets-by-reference across a whole flagship journey: zero secret-shaped values in every user-visible record pool), budget exhaustion (typed BUDGET_EXCEEDED for every counter, ACCOUNTING_UNKNOWN for incomplete accounting — never a guessed allowance, the denial audited, the cost ledger evidence-only, the exhausted task never fabricated completed)",
    "tests/accessibility/test/helpers.ts: the lane's composition boundary — fixed-literal spine fixtures (the W17 helper discipline), synthetic fragment-assembled secret fixtures, the product world (the §11 flagship composition over the merged public exports, mirroring the merged P13 exemplar), the resilience world (the P12-style durable-task + lease-supervision composition), the credential gateway (the P9 action gateway re-evaluating a P14 scoped credential at action time), and the shared evidence-graph truthfulness checks",
    "docs/evidence/productization/c-accessibility-negatives/scenarios/*.json: 16 machine-readable evidence records (one per scenario) binding each proof to the exact base and verified-head revisions with reproducibility (seed + command)"
  ],
  "removed": [],
  "modified": [],
  "preserved_invariants": [
    "no frozen contract is modified: spec/, docs/ (outside the owned evidence path), scripts/, apps/, packages/ and every sibling test suite are untouched — the lane owns ONLY tests/accessibility and docs/evidence/productization/c-accessibility-negatives; every product package's public exports are consumed read-only",
    "dogfood consumes public exports only: every composition in the lane assembles the merged packages' public surfaces exactly the way the product composes them (the P13 exemplar world pattern, the P12 autonomy world pattern, the P14 credential-gateway pattern, the P11 companion reference runtime) — no internal package API is reached; the ONE source binding is the entry-point-less P4 github provider, the documented P3/P4 lockfile precedent the merged P13 suite itself uses",
    "fail-closed authority everywhere: expired and revoked authority denies gated actions AT ACTION TIME (the executor never invoked), no privilege survives its authority at the gateway, at the promotion gate or mid-journey (the typed AUTHORITY_REQUIRED ask) — pinned by tests 05 and 06",
    "honest statuses everywhere: UNKNOWN where evidence is absent, UNAVAILABLE inside observed outage windows, never fabricated success or completion — pinned by tests 02, 03, 04, 08, 09, 11 and 15; the fresh-user completion itself is evidence-gated with retained plain-terms uncertainty",
    "secrets never appear in evidence or records: secrets are referenced BY NAME only; leakage attempts are detected and contained at the observation boundary (redaction audited / denial) and a whole-journey scan of every user-visible record pool finds zero secret-shaped values — pinned by test 14",
    "demo fixtures can never leak into the live evidence graph: the demo dataset is permanently typed (the demo-fixture provenance marker is bound into content-derived record identity), forged data sources are typed rejections, and a completed journey's live evidence graph contains zero demo markers and zero demo ids — pinned by test 12",
    "zero external dependencies: the lane package declares only workspace:* packages plus the repository's existing test toolchain (typescript/vitest/@types/node, the exact sibling versions); pnpm-workspace.yaml is untouched (tests/* already a glob) and pnpm-lock.yaml is byte-identical on the branch (the working-tree importer refresh during local install is reset before commit, the P3/P4/W17 precedent)",
    "offline determinism: no ambient time, randomness, environment, network, processes or timers in the owned sources (every clock, store, seam and probe injected; fixed literals throughout); the fixed vitest seed 424242; run-to-run identical results verified by running the full suite twice"
  ],
  "boundary_changes": [],
  "rationale_ref": "spec/productization-work-orders/P15-product-dogfood.md",
  "work_order": "P15"
}

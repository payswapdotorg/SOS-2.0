# Work Orders

Every Work Order is a bounded implementation contract.

Each defines dependencies, owned paths, scope, exclusions, acceptance criteria, deterministic verification and required evidence.

Workers stop at WAITING_FOR_ARCHITECT.

{
  "name": "@sos-2/conformance",
  "version": "0.1.0",
  "private": true,
  "description": "SOS 2.0 executable architecture invariants: typed invariant DSL (REQUIRED_INTERFACE, FORBIDDEN_DEPENDENCY, LAYERING, DATA_OWNERSHIP) with machine-checkable PASS/FAIL/NOT_APPLICABLE results, code-to-architecture reconciliation over the spine classifier, and Evidence-shaped drift records (spec/architecture.md sections 6-7).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@sos-2/semantic-spine": "workspace:*",
    "@sos-2/architecture": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}

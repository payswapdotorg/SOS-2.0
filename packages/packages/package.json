{
  "name": "@sos-2/packages",
  "version": "0.1.0",
  "private": true,
  "description": "SOS 2.0 Packages: evidence-backed reusable capability/subgraph artifacts with contracts, applicability, evidence, failures, assurance obligations, learned limitations, diversity profiles and the strict evidence-gated maturity lifecycle (one-lucky-success promotion rejected); context-conditioned applicability with calibrated-numeric discipline and the Beta-posterior baseline (Work Order W6; requirements R24, R26, R28).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "fixtures"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@sos-2/evidence": "workspace:*",
    "@sos-2/provenance": "workspace:*",
    "@sos-2/semantic-spine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@sos-2/contracts": "workspace:*",
    "ajv": "^8.17.1",
    "fast-check": "^3.23.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}

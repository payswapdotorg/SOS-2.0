{
  "name": "@sos-2/composition",
  "version": "0.1.0",
  "private": true,
  "description": "SOS 2.0 Composition: first-class PackageComposition artifacts (spine envelopes, frozen kind) with members and typed bindings, own-evidence validation independent of member evidence (the locked invariant), and the independence discipline — member probabilities are never multiplied without an explicit justification (Work Order W6; requirements R25, R27).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "fixtures"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@sos-2/evidence": "workspace:*",
    "@sos-2/packages": "workspace:*",
    "@sos-2/semantic-spine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}

{
  "name": "@sos-2/brownfield",
  "version": "0.1.0",
  "private": true,
  "description": "SOS 2.0 Brownfield Optimization Loop (Work Order W15, parallel slot C): a pure, deterministic orchestration of the merged SOS planes over an existing-system snapshot — repository/runtime ingestion (ImplementationModel + telemetry + runtime conformance), COMPETING architecture recovery (never one hypothesis under ambiguity), context-conditioned package retrieval with surfaced uncertainty, bounded candidate evolution (search + Pareto/quality-diversity + invariant-preserving subgraph replacement), assurance case evaluation with retained objections, fixed-seed SIMULATED experiment (never intervention evidence), evidence-gated promotion with ROLLBACK + bounded recovery declaration, implementation-vs-declared drift reconciliation, and package learning (transfer evidence, decay signals, failure memory) — with the traceability invariant that every stage handoff is a typed spine trace link and the full loop is ONE connected semantic subgraph from ImplementationModel to the learned ecology updates (spec/architecture.md sections 6 and 15, docs/code-to-architecture.md, R6/R23).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@sos-2/adapters": "workspace:*",
    "@sos-2/architecture": "workspace:*",
    "@sos-2/assurance": "workspace:*",
    "@sos-2/authority": "workspace:*",
    "@sos-2/conformance": "workspace:*",
    "@sos-2/ecology": "workspace:*",
    "@sos-2/evidence": "workspace:*",
    "@sos-2/experiments": "workspace:*",
    "@sos-2/memory": "workspace:*",
    "@sos-2/optimization": "workspace:*",
    "@sos-2/packages": "workspace:*",
    "@sos-2/promotion": "workspace:*",
    "@sos-2/provenance": "workspace:*",
    "@sos-2/registry": "workspace:*",
    "@sos-2/recovery": "workspace:*",
    "@sos-2/retrieval": "workspace:*",
    "@sos-2/runtime-conformance": "workspace:*",
    "@sos-2/search": "workspace:*",
    "@sos-2/semantic-spine": "workspace:*",
    "@sos-2/system-state": "workspace:*",
    "@sos-2/telemetry": "workspace:*",
    "@sos-2/transfer": "workspace:*",
    "@sos-2/verification": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}

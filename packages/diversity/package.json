{
  "name": "@sos-2/diversity",
  "version": "0.1.0",
  "private": true,
  "description": "SOS 2.0 Solution Diversity: the diversity ARCHIVE over the package/repertoire population — behavior-space coverage on the nine §11 dimensions realized as a §11-constrained MAP-Elites archive composing @sos-2/optimization's QD types (imported, never duplicated) — plus the diversity GUARDS that reject operations collapsing materially different high-performing families into one winner, and deterministic canonical coverage reports (Work Order W13; spec/architecture.md §11, §18; requirement R28; spec/architecture-lock.md: 'universal package winner replacing a diverse repertoire' is forbidden).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@sos-2/optimization": "workspace:*",
    "@sos-2/packages": "workspace:*",
    "@sos-2/semantic-spine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}

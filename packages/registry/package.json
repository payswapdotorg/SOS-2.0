{
  "name": "@sos-2/registry",
  "version": "0.1.0",
  "private": true,
  "description": "SOS 2.0 Registry: the package/composition registry — versioned spine-identity entries, linear supersession chains (superseded never returned as current, monotonic failure memory), evidence-gated promotion (the only maturity-changing path; one-lucky-success rejected), member compatibility + COMPOSES links for compositions, and altitude-ordered diversity-preserving retrieval by capability + context carrying uncertainty, evidence context, learned limitations and failure contexts (Work Order W6; requirements R24, R26, R28, R29).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@sos-2/composition": "workspace:*",
    "@sos-2/evidence": "workspace:*",
    "@sos-2/packages": "workspace:*",
    "@sos-2/provenance": "workspace:*",
    "@sos-2/semantic-spine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
